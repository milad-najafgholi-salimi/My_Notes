در زبان برنامه‌ نویسی R برای اینکه چندین شرط بررسی شوند و اگر چند شرط درست بودند، آن‌ها هم اجرا شوند، باید همگی را با if بنویسیم و از else if و else استفاده نکنیم. 

همچنین در R، یک else همیشه فقط به نزدیک‌ترین if قبلی که else ندارد وصل می‌شود.
