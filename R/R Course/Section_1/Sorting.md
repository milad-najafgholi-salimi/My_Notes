- For sorting from low to high you have learned to use `sort()` function. But sorting from low to high, you should use `decreasing = TRUE` option, like this:
```
num <- c(1, 5, 3, 8, 1, 2, 10, 0)

sort(num, decreasing = TRUE)

output:
	10  8  5  3  2  1  1  0
```
