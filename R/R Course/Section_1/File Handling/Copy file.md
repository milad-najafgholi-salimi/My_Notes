To copy a file to somewhere else, you need also to give the path if isn't in current working directory, then set the new path as the second parameter:
```
file.copy("path/file_name", "path/new_name")
```
`new_name` part is optional if you want to change the file's name.
