To work with csv files, first you need to make one with `file.create("file.csv")` - to save `iris` dataset on `iris.csv` file, look at the following code:
```
write.csv(iris, "iris.csv")
```
To read csv files use `read.csv("csv file")`:
```
read.csv("iris.csv")
```
